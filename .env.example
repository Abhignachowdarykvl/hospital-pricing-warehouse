# Copy to .env and fill in. Never commit .env.
POSTGRES_USER=hpw
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hospital_pricing
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CMS datasets (find the UUID on the dataset page -> "API" tab on data.cms.gov)
CMS_INPATIENT_DATASET_ID=
CMS_HOSPITAL_INFO_DATASET_ID=

# Optional: S3 raw zone (Step 3)
AWS_REGION=us-east-1
S3_BUCKET=
